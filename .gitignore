# dependencies
/node_modules
/.pnp
.pnp.js
.yarn/install-state.gz

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env*.local
.env

# secrets / agent prompt with credentials — never commit
AGENT-PROMPT.md

# Google Cloud credentials (OAuth clients, service account keys)
# Google's default download names are client_secret_*.json for OAuth clients
# and <project>-<keyhash>.json for service account keys.
client_secret*.json
*-service-account*.json
gcp-*.json
afrobirthday-*.json

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# Original video backups (after optimization)
/public/original-videos/

# Large source files (keep optimized versions only)
/public/*.MOV
/public/*.mov
.env*
!.env.example
